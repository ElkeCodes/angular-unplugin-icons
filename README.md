# Angular Unplugin Icons showcase

## Bundle analyzer (Angular 21)

Generate a production build with esbuild stats and create a visual bundle report that is opened in your browser:

- `npm run analyze` 
